# listit-cli
list something in a cli built using golang, just a project to really just learn golang.

# Getting Started
- `list it <"task">`: to add tasks to the list (`listit add` is an alternative)
- `list remove <"task">`: to remove tasks from the list
- `list ls`: to list all tasks in tabular format
- `list status`: to check number of tasks in the list
